<% if resource.errors.any? %>
  <% resource.errors.full_messages.each do |message| %>
    <%= turbo_stream.append 'flash_messages' do %>
      <%= render FlashComponent.new(type: 'error', data: message) %>
    <% end %>
  <% end %>
<% end %>
