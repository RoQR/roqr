<%= link_to path, class: "#{style_if_active(path, "text-slate-50", "text-slate-300 hover:text-slate-50") } group rounded-md px-3 py-2 flex items-center text-sm font-medium" do %>
  <div class="<%= style_if_active(path, "text-slate-50", "text-slate-300 group-hover:text-slate-50") %> flex-shrink-0 -ml-1 mr-3">
    <%= show_svg(icon) %>
  </div>
  <span class="truncate"> <%= name %> </span>
<% end %>
