<%= render_component 'card' do %>
  <%= form.simple_fields_for :telephone_link, @link.link_data || TelephoneLink.new do |telephone_form| %>
    <div class="md:grid md:grid-cols-3 md:gap-6">
      <div class="md:col-span-1">
        <h3 class="text-lg font-medium leading-6 text-gray-800 dark:text-gray-100">Link target</h3>
      </div>
      <div class="mt-5 md:mt-0 md:col-span-2">
        <div class="grid grid-cols-6 gap-6">
          <div class="col-span-12 sm:col-span-5 lg:col-span-3">
            <%= telephone_form.input :number, required: true %>
          </div>
        </div>
      </div>
    </div>
  <% end %>
<% end %>
