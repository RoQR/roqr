<%= form_with(model: link, class: "space-y-6") do |form| %>
  <%= render 'link_settings_fields', form: form, link: link %>
  <%= render @link_type, form: form %>
  <%= render 'link_styling_fields', form: form, link: link %>

  <div class="flex gap-x-3 justify-end">
    <%= link_to 'Cancel', :back, class: "inline-flex items-center px-4 py-2 border border-transparent text-gray-600 bg-gray-300 hover:bg-gray-400 focus:ring-gray-400 dark:text-gray-300 dark:bg-gray-600 dark:hover:bg-gray-500 dark:focus:ring-gray-500 text-sm font-medium rounded shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2" %>
    <button type="submit" class="inline-flex items-center px-4 py-2 border border-transparent text-gray-900 bg-yellow-400 hover:bg-yellow-500 focus:ring-yellow-500 text-sm font-medium rounded shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2">Save</button>
  </div>
<% end %>	
