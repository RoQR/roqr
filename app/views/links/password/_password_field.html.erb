<%= simple_fields_for @link do |form| %>
  <turbo-frame id="pw">
    <%= form.input :password, placeholder: 'Optional', optional: true , hint: 'Will prompt link scanner for password before redirecting' %>
  </turbo-frame>
<% end %>
