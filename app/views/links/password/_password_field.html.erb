<%= simple_fields_for @link do |form| %>
  <turbo-frame id="pw">
    <%= form.input :password, placeholder: 'Optional', optional: true , hint: 'Adding a password to your link will prompt the scanner for the password before redirecting them to the link target.' %>
  </turbo-frame>
<% end %>
