<% content_for :title, "Editing link" %>

<%= render "form", link: @link %>

<br>

<%= render InlineButtonGroupComponent.new do |c| %>
  <% c.with_button(link: links_path, style: 'white').with_content("Back to links") %>
  <% c.with_button(link: @link, style: 'white').with_content("Show this link") %>
<% end %>
