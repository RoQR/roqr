<!DOCTYPE html>
<html>
  <head>
    <%= render 'layouts/common_header' %>
    <script src="https://js.stripe.com/v3/"></script>
  </head>

  <body class="min-h-full h-screen bg-gray-50">
    <div>
      <%= render NavbarComponent.new(user: current_user) %>
      <header class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <% unless current_user.confirmed? %>
          <div class="rounded-md bg-yellow-50 p-4">
            <div class="flex flex-col max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
              <div>
                <h3 class="text-sm font-medium text-yellow-800">Email not verified</h3>
                <div class="mt-2 text-sm text-yellow-700">
                  <p>Please verify your email to enable link creation.</p>
                </div>
              </div>
            </div>
          </div>
        <% end %>
        <%= render_breadcrumbs builder: BreadcrumbsHelper::RoqrBreadcrumbs %>
      </header>
      <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
        <%= yield %>
      </div>
    </div>
  </body>
  <%= turbo_frame_tag "modal", target: :_top %>
  <ninja-keys data-controller="command-palette" hideBreadcrumbs></ninja-keys>
  <%= render 'layouts/flash' %>
</html>
