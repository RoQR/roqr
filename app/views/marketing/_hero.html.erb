<div  class="relative bg-gray-800 bg-texture overflow-hidden">
  <div class="relative py-6">
    <main class="my-16 sm:my-24">
      <div class="mx-auto px-2 max-w-7xl">
        <div class="pb-6 lg:grid lg:grid-cols-12 lg:gap-8">
          <div class="px-4 sm:px-6 sm:text-center md:max-w-2xl md:mx-auto lg:col-span-6 lg:text-left lg:flex lg:items-center">
            <div>
              <h1 class="mt-4 text-4xl tracking-tight font-extrabold text-white sm:mt-5 sm:leading-none lg:mt-6 lg:text-5xl xl:text-6xl">
                <span class="md:block">QR codes that will</span>
                <span class="text-yellow-300 md:block">rock your world</span>
              </h1>
              <p class="mt-3 text-base text-gray-300 sm:mt-5 sm:text-xl lg:text-lg xl:text-xl">Our supercharged QR codes let you manage, update and track behavior for all your marketing campaigns.</p>
            </div>
          </div>
          <div class="mt-16 sm:mt-24 lg:mt-0 lg:col-span-6">
            <div class="bg-gray-700 sm:max-w-md sm:w-full sm:mx-auto sm:rounded-lg sm:overflow-hidden">
              <div class="px-4 py-8 sm:px-10">
                <div>
                  <p class="text-sm font-medium text-gray-200">Sign in with</p>

                  <div class="mt-1 grid grid-cols-2 gap-3">

                    <div>
                      <%= render ButtonComponent.new(href: user_google_oauth2_omniauth_authorize_path, method: :post, classes: 'w-full justify-center', style: :secondary, data: { turbo: false }) do %>
                        <span class="sr-only">Sign in with Google</span>
                        <%= show_svg('google') %>
                      <% end %>
                    </div>
                    <div>
                      <%= render ButtonComponent.new(href: user_github_omniauth_authorize_path, method: :post, classes: 'w-full justify-center', style: :secondary, data: { turbo: false }) do %>
                        <span class="sr-only">Sign in with GitHub</span>
                        <%= show_svg('github') %>
                      <% end %>
                    </div>
                  </div>
                </div>

                <div class="mt-6 relative">
                  <div class="absolute inset-0 flex items-center" aria-hidden="true">
                    <div class="w-full border-t border-gray-600"></div>
                  </div>
                  <div class="relative flex justify-center text-sm">
                    <span class="px-2 bg-gray-700 text-gray-200"> Or </span>
                  </div>
                </div>

                <div class="mt-6">
                  <%= form_for(User.new, as: :user, url: registration_path(:user), html: { class: "mt-8 space-y-6" }) do |f| %>
                    <input type="hidden" name="remember" value="true">
                    <div class="rounded-md shadow-sm">
                      <div>
                        <%= f.label :name, class: "sr-only" %>
                        <%= f.text_field :name, required: true, class: "appearance-none relative block w-full px-3 py-2 border bg-gray-800 focus:bg-gray-700 border-transparent focus:border-gray-100 text-gray-100 rounded-t-md  sm:text-sm", placeholder: "Name" %>
                      </div>
                      <div class="mt-px">
                        <%= f.label :email, class: "sr-only" %>
                        <%= f.email_field :email, required: true, class: "appearance-none relative block w-full px-3 py-2 border bg-gray-800 focus:bg-gray-700 border-transparent focus:border-gray-100 text-gray-100 rounded-b-md  sm:text-sm", placeholder: "Email" %>
                      </div>
                    </div>

                    <div>
                      <%= render_component 'button', type: :submit, classes: 'w-full justify-center', data: { turbo: false } do %>
                        Get 30 days free
                      <% end %>
                    </div>
                  <% end %>
                </div>
              </div>
              <div class="px-4 py-6 bg-gray-700 border-t-2 border-gray-600 sm:px-10">
              <p class="text-xs leading-5 text-gray-300">By signing up, you agree to our <a class="font-medium text-gray-200 hover:underline" href="https://docs.roqr.app/terms-of-service">Terms of Service</a> and <a class="font-medium text-gray-200 hover:underline" href="https://docs.roqr.app/privacy-policy">Privacy Policy</a>.</p>
              </div>
            </div>
          </div>
        </div>
        <h2 class="my-4 text-2xl tracking-tight font-extrabold text-white text-center sm:my-5 sm:leading-none lg:my-6 lg:text-3xl xl:text-4xl">
          Create free static QR codes without signing up
        </h2>
        <%= turbo_frame_tag :qr, src: "/marketing/qr/type" do %>
        <% end %>
      </div>
    </main>
  </div>
</div>
