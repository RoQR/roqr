<%= render_component 'card' do %>
  <%= form_with(model: @qr, url: wizard_path, method: :put) do |f| %>
    <div class="mt-5 md:mt-0 md:col-span-2">
      <div class="grid grid-cols-6 gap-6">
        <div class="col-span-3">
          <div class="flex">
            <%= f.label :ssid, "Network name", class: "block text-sm font-medium text-gray-600 dark:text-gray-300" %>
            <%= render TooltipComponent.new do %>
              Sometimes referred to as an SSID.
            <% end %>
          </div>
          <div class="mt-1">
            <%= f.text_field :ssid, class: "bg-gray-200 focus:bg-gray-100 dark:bg-gray-800 dark:focus:bg-gray-700 border-transparent focus:border-gray-100 text-gray-800 dark:text-gray-100 block w-full sm:text-sm rounded-md", required: true %>
          </div>
        </div>

        <div class="col-span-2">
          <%= f.label :password, class: "block text-sm font-medium text-gray-600 dark:text-gray-300" %>
          <div class="mt-1">
            <%= f.password_field :password, class: "bg-gray-200 focus:bg-gray-100 dark:bg-gray-800 dark:focus:bg-gray-700 border-transparent focus:border-gray-100 text-gray-800 dark:text-gray-100 block w-full sm:text-sm rounded-md" %>
          </div>
        </div>

        <div class="col-span-3">
          <div class="mb-3 flex flex-col justify-between">
              <div class="flex">
                <label class="text-base font-medium text-gray-600 dark:text-gray-300">Security protocol</label>
                <%= render TooltipComponent.new do %>
                  Most WiFi networks have some form of protection through a security protocol. The most common form of security protocol is WEP, but WPA is also becoming more popular. To find out your settings, check your network settings through a device that is already connected to the network.
                <% end %>
              </div>
            <fieldset class="mt-4">
              <legend class="sr-only">Wifi security protocol</legend>
              <div class="space-y-4 sm:flex sm:items-center sm:space-y-0 sm:space-x-10">
                <div class="flex items-center">
                  <%= f.radio_button :protocol, nil, class: "focus:ring-gray-700 bg-gray-300 text-gray-300 dark:bg-gray-800 h-4 w-4 dark:text-gray-800 border-gray-300" %>
                  <%= f.label :protocol, "None", class:"ml-3 block text-sm font-medium text-gray-700 dark:text-gray-200"  %>
                </div>

                <div class="flex items-center">
                  <%= f.radio_button :protocol, "WEP", class: "focus:ring-gray-700 bg-gray-300 text-gray-300 dark:bg-gray-800 h-4 w-4 dark:text-gray-800 border-gray-300" %>
                  <%= f.label :protocol, "WEP", class:"ml-3 block text-sm font-medium text-gray-700 dark:text-gray-200"  %>
                </div>

                <div class="flex items-center">
                  <%= f.radio_button :protocol, "WPA", class: "focus:ring-gray-700 bg-gray-300 text-gray-300 dark:bg-gray-800 h-4 w-4 dark:text-gray-800 border-gray-300" %>
                  <%= f.label :protocol, "WPA", class:"ml-3 block text-sm font-medium text-gray-700 dark:text-gray-200"  %>
                </div>
              </div>
            </fieldset>
          </div>
        </div>
      </div>
    </div>
    <%= render_component 'button', type: :submit, classes: 'mt-3' do %>
      Continue
    <% end %>
  <% end %>
<% end %>
