<div class="pt-6">
  <div class="flow-root rounded-lg bg-gray-300 px-6 pb-8">
    <div class="-mt-6">
      <div>
        <span class="inline-flex items-center justify-center rounded-md bg-black p-3 shadow-lg text-yellow-300">
          <%= show_svg(@icon) %>
        </span>
      </div>
      <h3 class="mt-8 text-lg font-medium tracking-tight text-gray-800"><%= @title %></h3>
      <p class="mt-5 text-base text-gray-900"><%= content %></p>
    </div>
  </div>
</div>
