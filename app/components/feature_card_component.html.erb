<div class="pt-6">
  <div class="flow-root rounded-lg bg-slate-50 px-6 pb-8">
    <div class="-mt-6">
      <div>
        <span class="inline-flex items-center justify-center rounded-md bg-indigo-500 p-3 shadow-lg text-white">
          <%= show_svg(@icon) %>
        </span>
      </div>
      <h3 class="mt-8 text-lg font-medium tracking-tight text-slate-900"><%= @title %></h3>
      <p class="mt-5 text-base text-slate-500"><%= content %></p>
    </div>
  </div>
</div>
