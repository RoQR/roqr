<%= button_to @link, method: @method, type: "button", class: "inline-flex items-center #{px} #{py} border #{styling} #{text_size} font-medium rounded shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2", data: @data do %>
  <%= content %>
<% end %>
