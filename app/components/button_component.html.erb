<%= button_to @link, method: @method, class: "inline-flex items-center #{px} #{py} border #{styling} #{text_size} font-medium rounded shadow-sm focus:outline-none focus:ring-2 focus:ring-offset-2" do %>
  <%= content %>
<% end %>
